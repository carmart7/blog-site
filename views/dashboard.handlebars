{{#each blogs as |blog|}}
<a href="/dashboard/edit/{{id}}">
    <div class="card text-center m-3">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h2>{{title}}</h2>
            <h3>{{format_date createdAt}}</h3>
        </div>
    </div>
</a>
{{/each}}

<a href="/dashboard/create"><button type="button" class="btn btn-secondary">Create Post</button></a>